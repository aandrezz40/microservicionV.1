server:
  port: 8761

spring:
  application:
    name: eureka-service

  config:
    import: "optional:configserver:http://localhost:8084"

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
